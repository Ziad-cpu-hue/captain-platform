name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.0'

      - name: Get dependencies
        run: cd captain_platform/flutter_app && flutter pub get

      - name: Build APK
        run: cd captain_platform/flutter_app && flutter build apk --debug

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: captain-platform-apk
          path: captain_platform/flutter_app/build/app/outputs/flutter-apk/app-debug.apk
